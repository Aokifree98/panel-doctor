import { Title } from '@angular/platform-browser';

export interface ListAntHeredoFamiliarCita {
  id?: number,
  Condition?: string,
  DetallecitaId?: number,
  AntecedenteheredofamiliarId?: number
}
