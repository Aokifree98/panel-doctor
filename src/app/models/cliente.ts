import { Title } from '@angular/platform-browser';

export interface Cliente {
  id?: number,
  Name?: string,
  LastName?: string,
  BirthDate?: Date,
  Job?: string,
  Direction?: string,
  Phone?: string,
  Gender?: string,
  CivilStatus?: string,
  DocumentNumber?: string,
  Email?: string,
  Password?: string,
  Photo?: string,
  Google?: string,
  Condition?: string,
  Code?: string,
}
